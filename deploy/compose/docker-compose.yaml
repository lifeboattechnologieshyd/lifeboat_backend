services:
  postgres:
    image: postgres:17
    container_name: lifeboat-postgres
    restart: unless-stopped

    env_file:
      - ../../.env.docker

    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      PGSSLMODE: ${PGSSLMODE:-disable}

    ports:
      - "${POSTGRES_PORT_HOST:-5437}:5432"

    volumes:
      - pg_data:/var/lib/postgresql/data

    networks:
      - lifeboat-network

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    image: lifeboat-backend:latest

    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
      tags:
        - lifeboat-backend:latest

    container_name: lifeboat-backend
    restart: unless-stopped

    env_file:
      - ../../.env

    depends_on:
      postgres:
        condition: service_healthy

    ports:
      - "${HTTP_PORT:-8015}:8000"

    networks:
      - lifeboat-network

    healthcheck:
      test: ["CMD", "python", "-c", "print('healthy')"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pg_data:
    external: true
    name: lifeboat_pg_data

networks:
  lifeboat-network:
    external: true